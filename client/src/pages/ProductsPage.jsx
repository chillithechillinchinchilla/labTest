import React from "react";
import ListProducts from "../components/ListProducts";

const ProductsPage = () => {
    return (
        <div>
            <ListProducts />
        </div>
    );
};

export default ProductsPage;
